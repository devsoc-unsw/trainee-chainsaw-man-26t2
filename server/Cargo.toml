[package]
edition = "2024"
name = "server"
version = "0.1.0"

[dependencies]
axum = "0.8.9"
sqlx = { version = "0.9.0", features = [
    "chrono",
    "postgres",
    "runtime-tokio",
    "tls-rustls",
    "uuid",
] }
tokio = { version = "1.53.1", features = ["macros"] }
